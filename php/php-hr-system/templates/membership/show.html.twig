{% extends 'base.html.twig' %}

{% block title %}Membership{% endblock %}

{% block body %}
    <h1>Membership</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membership.id }}</td>
            </tr>
            <tr>
                <th>Employee</th>
                <td>{{ membership.Person.FamilyName }} {{ membership.Person.FirstName }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ membership.Unit.Name }}</td>
            </tr>
            <tr>
                <th>WorkingHoursPerWeek</th>
                <td>{{ membership.WorkingHoursPerWeek }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('membership_index') }}">back to list</a>

    <a href="{{ path('membership_edit', {'id': membership.id}) }}">edit</a>

    {{ include('membership/_delete_form.html.twig') }}
{% endblock %}
