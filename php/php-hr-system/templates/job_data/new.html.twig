{% extends 'base.html.twig' %}

{% block title %}New JobData{% endblock %}

{% block body %}
    <h1>Create new JobData</h1>

    {{ include('job_data/_form.html.twig') }}

    <a href="{{ path('job_data_index') }}" class="btn btn-primary">back to list</a>
{% endblock %}
