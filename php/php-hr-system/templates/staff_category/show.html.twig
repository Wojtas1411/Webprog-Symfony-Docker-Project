{% extends 'base.html.twig' %}

{% block title %}StaffCategory{% endblock %}

{% block body %}
    <h1>StaffCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ staff_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ staff_category.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('staff_category_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('staff_category_edit', {'id': staff_category.id}) }}" class="btn btn-primary">edit</a>

    {{ include('staff_category/_delete_form.html.twig') }}
{% endblock %}
